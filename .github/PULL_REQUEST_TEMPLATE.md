## Summary

<!-- What changed and why (1–3 bullets). -->

-

## Test plan

- [ ] `python scripts/format_source.py --check`
- [ ] `python scripts/build_repo.py --no-demos --test`（无 Qt 协议/规则单测）
- [ ] （若改 Host/Demo）`python scripts/build_repo.py --test` + 手工拖出/合入烟测

## Notes

<!-- Spec / Demo IPC / breaking changes, if any. -->
